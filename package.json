{
  "name": "dog-platform",
  "version": "1.0.0",
  "description": "Dog platform with pet shop registration",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "cd frontend && npm run build"
  },
  "dependencies": {
    "airtable": "^0.12.2"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0"
  }
}
